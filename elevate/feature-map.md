# D2D Platform — Feature Map

> Generated by `/elevate` dry-run · 2026-06-09 · Overall maturity: **35 / 100**

| Feature                | Entry Path                                   | Data Wired                                                                                      | Current Score | Top Gap                                                                                                                 |
| ---------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Command Centre         | `/command-centre`                            | Partial (fleet API exists; activity route returns 0 rows → seed fallback; anomalies hardcoded)  | **26/100**    | Activity feed polls a route returning 0 rows → seed fallback; anomaly "Reassign" button has no onClick                  |
| Roster                 | `/accounts/[slug]/roster`                    | No (`/api/shifts` built but never called; all state is ephemeral `useState`)                    | **41/100**    | Hardcoded week anchor (May 19 2026), static REPS array, zero persistence across page refresh                            |
| Marketing Studio       | `/accounts/[slug]/marketing-studio/generate` | Partial (POST fires but silent-fails to seeds; approve/queue stubs never call API)              | **23/100**    | Silent fallback to `VARIANT_SEEDS` on any API error; "Send to review queue" button has no onClick                       |
| Auth + Onboarding      | `/login`, `/onboard-account`                 | Yes (session layer correct; `/api/orgs` POST wired end-to-end)                                  | **47/100**    | Open redirect on `?next=`, no rate limiting, JWT shape-only validation in middleware, no password reset                 |
| Territory Intel        | `/accounts/[slug]/territory-intel`           | No (heatmap reads from fixture GeoJSON; no live DB query)                                       | **~30/100**   | Propensity scores hardcoded; no live `Territory` table reads; AI zone suggestions are string constants                  |
| Knocker iOS            | `/accounts/[slug]/knocker-ios`               | No (HTML mockup; no Swift binary; APNs config is aspirational text)                             | **~15/100**   | No native app exists; "TestFlight · Internal Track" label is false; all config writes are no-ops                        |
| Leads Inbox            | `/accounts/[slug]/leads`                     | Yes (best-designed page; PII masking live; Fixture/Live badge correct)                          | **~72/100**   | PII vault decrypt path not shown in audit; no bulk-action wiring                                                        |
| Compliance             | `/accounts/[slug]/compliance`                | Partial (backend fully built; UI reads fixture file not live `PaidSolicitorRegistration` table) | **~55/100**   | 50-state matrix reads fixture; no registration-filing UI; `assertStateCleared()` hard-gate correct but untested from UI |
| Pipeline / CRM         | `/accounts/[slug]/pipeline`                  | No (stage moves fire console.log; no PATCH to API)                                              | **~28/100**   | All Kanban drag-drop mutations are local state only                                                                     |
| A&E Group Intelligence | `/` (A&E app)                                | Partial (read-only ledger; append-only correct; currency consolidation logic present)           | **~60/100**   | Stray duplicate Postgres instance; PostGIS columns still TEXT strings                                                   |

## Scorecard Distribution

- 🔴 Under 30 (demo-grade): Command Centre, Marketing Studio, Territory Intel, Knocker iOS, Pipeline
- 🟡 30–55 (partial): Roster, Auth+Onboarding, Compliance
- 🟢 55+ (functional): Leads Inbox, A&E Intelligence

## Fastest path to pilot-readiness

The three features Hope Forward needs on Day 1 are: Command Centre (monitor 200+ knockers), Roster (schedule shifts), and Compliance (state clearance). All three are currently below 50. The Leads Inbox (72) is already pilot-grade. Fix the bottom three and the platform is demo-ready for a real paying customer.

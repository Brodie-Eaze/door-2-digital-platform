{
  "name": "@d2d/database",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Shared Prisma client singleton + type re-exports. Schema lives at apps/api/prisma/schema.prisma — this package consumes the generated @prisma/client and provides a single-instance client safe to import from any app (web-operator route handlers, api workers, etc.).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\""
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
